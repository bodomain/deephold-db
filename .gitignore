# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.ENV
ENV/

# --- Poetry ---------------------------------------------------------------
# poetry.lock ist eingecheckt; .venv-Outputs nicht
.venv*/

# --- Test / Coverage ------------------------------------------------------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.cache

# --- IDE / Editoren -------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# --- Jupyter / Notebooks --------------------------------------------------
.ipynb_checkpoints/
*.ipynb
# Notebook outputs (rebuilt by scripts/build_notebook.py)
notebooks/*.html

# --- Bun / OpenTUI-TUI ----------------------------------------------------
# node_modules + Build-Caches; bun.lock wird eingecheckt (Reproduzierbarkeit)
tui/node_modules/
tui/dist/
tui/coverage/
# Falls jemand ein anderes Lockfile-Format erzeugt:
tui/bun.lockb

# --- Secrets & lokale Overrides ------------------------------------------
.env
.env.local
.env.*.local

# --- Daten / Caches (lokal) ----------------------------------------------
data/
downloads/
.parquet/
*.parquet
*.csv.gz

# --- Prefect --------------------------------------------------------------
.prefect/

# --- Docker ---------------------------------------------------------------
# Volumes werden in docker-compose volumes: {} definiert, kein Bind-Mount.

# --- MyPy / Ruff ----------------------------------------------------------
.mypy_cache/
.ruff_cache/

# --- LaTeX Build-Artifacts (nur PDF + Source einchecken) -------------------
docs/handbuch/*.aux
docs/handbuch/*.bcf
docs/handbuch/*.idx
docs/handbuch/*.ilg
docs/handbuch/*.ind
docs/handbuch/*.lof
docs/handbuch/*.log
docs/handbuch/*.lot
docs/handbuch/*.out
docs/handbuch/*.run.xml
docs/handbuch/*.toc
docs/handbuch/*.bbl
docs/handbuch/*.blg
docs/handbuch/*.synctex.gz
# Alte Finance_Data-Reste (vor Rename) — nicht einchecken
docs/handbuch/finance_data.*

# --- Screenshots / temp ---------------------------------------------------
*.png
